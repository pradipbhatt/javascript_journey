let count=1; //count starts from 1
 
while (count<15)  //execute the inside commands until condition fails.

 {
    console.log("I am an engineer"+count);
    count+=1;
}

// While loop is used only when we set the ending condition
