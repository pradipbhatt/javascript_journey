// In JS, there are many built-in methods available for different data types. For example, the sort() method is available for arrays, which sorts the array elements in ascending order.

let number = '1,2,3,4,5';
let result = parseInt(number);
console.log(result);